/**
 * Returns the first half of a parameter string.
 * 
 * @param {String} str - A string parameter
 * @returns {String} - Returns the first half of a string
 */
function firstHalf (str) {
  return str.substring(0, str.length / 2);
}