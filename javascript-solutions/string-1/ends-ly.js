/**
 * Checks if a string ends in 'ly'.
 * 
 * @param {String} str - A string parameter
 * @returns {Boolean} - Returns a Boolean
 */
function endsLy (str) {
  return str.substring(str.length - 2) === 'ly';
}