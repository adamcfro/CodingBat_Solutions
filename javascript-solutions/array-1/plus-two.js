/**
 * Returns a single array containing all elements from both parameters.
 * 
 * @param {Array} arr1 - First array parameter 
 * @param {Array} arr2 - Second array parameter
 * @returns {Array} - Returns a concatenated array
 */
function plusTwo (arr1, arr2) {
  return arr1.concat(arr2);
}