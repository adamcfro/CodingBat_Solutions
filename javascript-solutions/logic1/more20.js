/**
 * Returns true if parameter is 1 or 2 more than a multiple of 20.
 * 
 * @param {Number} num        A number parameter
 * @returns {Boolean}         Returns a Boolean
 */
function more20 (num) {
  return (num % 20 === 1 || num % 20 === 2);
}