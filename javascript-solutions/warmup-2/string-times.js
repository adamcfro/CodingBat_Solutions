/**
 * Returns a string that is n copies of the original string
 * 
 * @param {String} str - A string parameter
 * @param {Number} n - Number of copies of string
 * @returns {String} - Returns new string
 */
function stringTimes (str, n) {
  return str.repeat(n);
}